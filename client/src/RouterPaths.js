const welcomePage = '/';

const settingsPage = '/settings';

const homePage = '/home';

const spotifyAuthFlowPath = '/spotifyAuthFlow';

const paths = {
  welcomePage,
  settingsPage,
  homePage,
  spotifyAuthFlowPath,
};

export default paths;
