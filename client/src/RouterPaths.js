const welcomePage = '/';

const accountPage = '/account';

const homePage = '/home';

const spotifyAuthFlowPath = '/spotifyAuthFlow';

const paths = {
  welcomePage,
  accountPage,
  homePage,
  spotifyAuthFlowPath,
};

export default paths;
